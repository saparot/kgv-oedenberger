<form method="post" action="{{ path('download_file_delete', {'id': download_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ download_file.id) }}">
    <button class="btn">Delete</button>
</form>
