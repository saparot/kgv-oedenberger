{% extends 'base-administration.html.twig' %}

{% block title %}Executive{% endblock %}

{% block body %}
    <h1>Executive</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Anzeige Reihenfolge</th>
            <td>{{ executive.sort }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ executive.name }}</td>
        </tr>
        <tr>
            <th>Position</th>
            <td>{{ executive.position }}</td>
        </tr>

        <tr>
            <th>E-Mail</th>
            <td>{{ executive.email }}</td>
        </tr>
        <tr>
            <th>Telefon</th>
            <td>{{ executive.phone }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('administrationExecutive') }}">zurück</a>

    <a href="{{ path('administrationExecutiveEdit', {'id': executive.id}) }}">bearbeiten</a>

    {# {{ include('administration/executive/_delete_form.html.twig') }} #}
{% endblock %}
