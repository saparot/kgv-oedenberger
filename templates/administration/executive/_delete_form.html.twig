<form method="post" action="{{ path('administrationExecutiveDelete', {'id': executive.id}) }}" onsubmit="return confirm('Bist Du dir sicher diese Vorstandsposition loeschen zu ' +
 'wollen?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ executive.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
